# Contributing

## Setup
1. Clone repo
2. Run `npm install`
3. Copy `.env.example` to `.env.local`
4. Run `npm run dev`

## PR Process
1. Create feature branch
2. Make changes
3. Open PR against master
